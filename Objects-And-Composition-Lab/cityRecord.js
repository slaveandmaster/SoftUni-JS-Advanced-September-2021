function solve(name, population, treasury) {
    const city = {
        name,
        population,
        treasury
    }
    console.log(city);
}
solve("Tortuga", 7000, 15000);
solve('Santo Domingo',
12000,
23500
);